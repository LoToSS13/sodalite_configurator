# Sodalite Configurator

Offline Flutter web editor for Sodalite configurable module packs. Labels are in Russian; exported JSON keys match Consul.

## Run

```bash
flutter run -d chrome --web-renderer html
```
